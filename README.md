before open the project:
npm install

to see the project:
npm run dev

Llamada a componentes, Ejemplos:

> > Components > Card:

      <Card
        id={id}
        name={spanishName}
        img={clowReverse}
        text={meaning}
        alt={meaning}
      />
